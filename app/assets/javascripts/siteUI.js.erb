$(document).on('turbolinks:load', function() {
  $('.modal_impression').modaal({
    overlay_opacity: 0.3,
    overlay_close: 'false',
    hide_close: 'true',
    close_text: '決定',
    animation: 'none',
    content_source: '#modal_impression',
    inject_position: '#blog_profile'
  });
  $('.btn_notice').modaal({
    overlay_opacity: 0.3,
    overlay_close: 'true',
    start_open: true,
    hide_close: 'true',
    close_text: '×',
    animation: 'none',
    content_source: '#modal_notice',
    inject_position: 'body'
  });
  autosize($('textarea'));

  // $(window).on('load resize', function(){
  //   pagenationResize();
  // });
  // $(document).on('click', '.pagination span', function (){
  //   pagenationResize();
  // });




});
function editTarget(id) {
  let comment = gon.blog_comments.find(item => item['id'] == id);
  $('#edit_impression_comment').val(comment['content']);
  $('#edit_impression').attr('action', '/comments/' + id );
}
// function pagenationResize(){
//   let cell_size = 0;
//   let cell_space = $('.pagination').width();
//
//   $('.pagination span').each(function(index, element) {
//     console.log($(this).attr('class'));
//     _this = $(this);
//
//     if (_this.attr('class').match(/page/)) {
//       cell_size ++ ;
//     }else{
//       cell_space -= _this.width();
//     }
//   });
//   $('.pagination span.page').css({
//     'width': cell_space / cell_size
//   });
//
// }
